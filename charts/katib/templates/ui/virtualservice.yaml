{{- if (include "kubeflow.katib.enabled" . | eq "true") -}}
{{- if .Values.istioIntegration.create }}
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  labels:
    {{- include "kubeflow.katib.ui.labels" . | nindent 4 }}
  name: {{ include "kubeflow.katib.ui.name" . }}
  namespace: {{ include "kubeflow.namespace" . }}
spec:
  gateways:
    - {{ .Values.istioIntegration.gateway.name }}
  hosts:
    - '*'
  http:
    - match:
        - uri:
            prefix: {{ .Values.urlPrefix }}/
      rewrite:
        uri: /
      route:
        - destination:
            host: {{ include "kubeflow.katib.svc.fqdn" . }}
            port:
              number: 80
      headers:
        request:
          add:
            x-forwarded-prefix: {{ .Values.urlPrefix }}
            Tracing-Context: {{ include "kubeflow.katib.ui.baseName" . }}
        response:
          add:
            Tracing-Context: {{ include "kubeflow.katib.ui.baseName" . }}
{{- end }}
{{- end }}