{{- if (include "kubeflow.notebooks.modelsWebApp.createServiceAccount" . | eq "true") -}}

apiVersion: v1
kind: ServiceAccount
metadata:
  {{- with .Values.notebooks.modelsWebApp.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "kubeflow.notebooks.modelsWebApp.labels" . | nindent 4 }}
  name: {{ include "kubeflow.notebooks.modelsWebApp.serviceAccountName" . }}
  namespace: {{ include "kubeflow.namespace" . }}

{{- end }}
