{{- if (include "kubeflow.notebooks.modelsWebApp.enabled" . | eq "true") -}}

apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  labels:
    {{- include "kubeflow.notebooks.modelsWebApp.labels" . | nindent 4 }}
  name: {{ include "kubeflow.notebooks.modelsWebApp.name" . }}
  namespace: {{ include "kubeflow.namespace" . }}
spec:
  action: ALLOW
  selector:
    matchLabels:
      {{- include "kubeflow.notebooks.modelsWebApp.selectorLabels" . | nindent 6 }}
  rules:
  - from:
    - source:
        namespaces:
        - {{ .Values.istioIntegration.ingressGatewayNamespace }}

{{- end }}
