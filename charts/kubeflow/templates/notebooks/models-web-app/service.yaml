{{- if (include "kubeflow.notebooks.modelsWebApp.enabled" . | eq "true") -}}

apiVersion: v1
kind: Service
metadata:
  {{- with .Values.notebooks.modelsWebApp.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "kubeflow.notebooks.modelsWebApp.labels" . | nindent 4 }}
  name: {{ include "kubeflow.notebooks.modelsWebApp.svc.name" . }}
  namespace: {{ include "kubeflow.namespace" . }}
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    {{- include "kubeflow.notebooks.modelsWebApp.selectorLabels" . | nindent 4 }}
  type: {{ .Values.notebooks.modelsWebApp.service.type }}

{{- end }}
