{{- if (include "kubeflow.notebooks.modelsWebApp.enabled" . | eq "true") -}}

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    {{- include "kubeflow.notebooks.modelsWebApp.labels" . | nindent 4 }}
  name: {{ include "kubeflow.notebooks.modelsWebApp.mainClusterRoleBindingName" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "kubeflow.notebooks.modelsWebApp.mainClusterRoleName" . }}
subjects:
- kind: ServiceAccount
  name: {{ include "kubeflow.notebooks.modelsWebApp.serviceAccountName" . }}
  namespace: {{ include "kubeflow.namespace" . }}

{{- end }}
