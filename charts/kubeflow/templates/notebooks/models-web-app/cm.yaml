{{- if (include "kubeflow.notebooks.modelsWebApp.enabled" . | eq "true" ) -}}

apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    {{- include "kubeflow.notebooks.modelsWebApp.labels" . | nindent 4 }}
  name: {{ include "kubeflow.notebooks.modelsWebApp.configMapName" . }}
  namespace: {{ include "kubeflow.namespace" . }}
data:
  APP_DISABLE_AUTH: "True"
  USERID_HEADER: {{ .Values.auth.userHeaderName | quote }}
  APP_PREFIX: "{{ .Values.notebooks.modelsWebApp.config.urlPrefix }}"

{{- end }}
