{{- if and .Values.centraldashboard.enabled .Values.centraldashboard.rbac.serviceAccount.create -}}

apiVersion: v1
kind: ServiceAccount
metadata:
  {{- with .Values.centraldashboard.rbac.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4}}
  {{- end }}
  labels:
    {{- include "kubeflow.centraldashboard.labels" . | nindent 4 }}
  name: {{ include "kubeflow.centraldashboard.rbac.serviceAccountName" . }}
  namespace: {{ include "kubeflow.namespace" . }}

{{- end }}