apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kubeflow

bases:
- ../kubeflow

configMapGenerator:
- literals:
  - USERID_HEADER="x-auth-request-email"
  behavior: merge
  name: config
